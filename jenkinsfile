pipeline {
    agent any

    stages {
        stage('Run docker nginx') {
            steps {
                sh 'mkdir -p /var/www'
                sh 'cp ./www/index.html /var/www/index.html'
                sh 'docker-compose up -d'
            }
        }
    }
}